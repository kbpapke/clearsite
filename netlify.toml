# ClearSite — Netlify configuration

[build]
  publish = "."
  functions = "netlify/functions"

# Sensible security headers for a static page
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
